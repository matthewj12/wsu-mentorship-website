manually merged in admin dashboard from admin-dashboard branch

fixed problem where "cancel" button for delete match dialogue was disabled

fixed key-error in createMatches() that occured when, for example, a mentor had a mentee in their ranking that wasn't currently being considered for matching